package by.academy.classwork.test;

import by.academy.classwork.test.editor.Editor;
import by.academy.classwork.test.listeners.EmailNotificationListener;
import by.academy.classwork.test.listeners.LogOpenListener;

import java.util.LinkedList;
import java.util.List;
import java.util.Set;
import java.util.stream.Stream;

public class Demo {
    public static void main(String[] args) {
       // System.arraycopy();
        /*Editor editor = new Editor();
        editor.events.subscribe("open", new LogOpenListener("/path/to/log/file.txt"));
        editor.events.subscribe("save", new EmailNotificationListener("admin@example.com"));

        try {
            editor.openFile("test.txt");
            editor.saveFile();
        } catch (Exception e) {
            e.printStackTrace();
        }*/
//        List.of(100, 222, 55).stream().peek(s -> System.out.println(s))
//                .forEach(s -> System.out.println(s));
    }

}
